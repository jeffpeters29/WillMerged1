@page
@model Qwill.Pages.Step2Model
@{
    ViewData["Title"] = "Step2";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Step2</h2>

<div class="container">
    <div class="row">

        <div class="col-6 offset-3">
            <div>
                <partial name="_ErrorMessages" for="ErrorMessage" />

                <form method="post" asp-page-handler="Step2Form">
                    <input type="hidden" asp-for="WillInfo.Id" />

                    <div class="mb-3">
                        <h3>Do you have any children under 18?</h3>
                    </div>

                    <div class="mb-5">
                        <a class="btn btn-primary btn-block" data-toggle="modal" data-target="#childModal">Add Child</a>
                    </div>
               
                    <button class="btn btn-primary btn-block" type="submit">Next</button>
                </form>

            </div>
        </div>

    </div>
</div>

<!--Child Modal-->
<div id="childModal" class="modal fade responsiveModal" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body">
                    @*<input type="hidden" asp-for="ChildInfo.Id" />

                    <div class="mb-3">
                        <label asp-for="ChildInfo.FirstName"></label>
                        <input asp-for="ChildInfo.FirstName" class="form-control" placeholder="Name *" />
                        <span asp-validation-for="ChildInfo.FirstName" class="text-danger"></span>
                    </div>

                    <div class="mb-5">
                        <label asp-for="ChildInfo.Over18"></label>
                        <input asp-for="ChildInfo.Email" class="form-control" placeholder="Name *" />
                        <span asp-validation-for="ChildInfo.Email" class="text-danger"></span>
                    </div>*@

                    <button class="btn btn-primary btn-block" type="submit">Save</button>
            </div>
        </div>
    </div>
</div>